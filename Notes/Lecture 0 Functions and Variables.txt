Lecture 0- Functions & Variables:

Ctrl+N to open new file in a folder in vs code.
Shift+Enter to run python file in vs code
Functions:
words,actions or it allows you to do something in the programming language.
Arguments:
Input to a function has some influence on its behaviour.
Side Effects:
Printing something on the screen like Hello world is called side effects.
Bug:
Mistake in a programm.
Input Function:
https://docs.python.org/3/library/functions.html#input
https://docs.python.org/3/tutorial/inputoutput.html
Return Values:
def add(a, b):
    return a + b

result = add(2, 3)
print(result)
5
💡 In Simple Words
Function = machine 🏭
Input = numbers you give
Return value = answer you get back
🔑 One Line Definition

👉 Return value is the output of a function

Variables:
🔹 Easy Example (Python)
x = 10
name = "Ali"
x stores number 10
name stores text "Ali"
💡 In Simple Words

👉 A variable is like a box 📦 where you keep data
👉 The name of the box = variable
👉 The thing inside = value

🔑 One Line Definition

👉 A variable is a container that stores data

Assignment operator:
Assignment operator (=) puts a value inside a variable

Think like:
📦 x = 10 → put 10 inside box x

Comments:
Comments are notes in your code that Python ignores
👉 They are only for humans to understand the code

🔹 Single-Line Comment

Use #

# This is a comment
x = 10  # This stores value 10

🔹 Multi-Line Comment

Use triple quotes:

"""
This is a multi-line comment
You can write many lines here
"""

pseudocode:
just human language to express your thoughts.
good for structuring to do list

Concatenation:
👉 Concatenation means joining two or more things together
It is mostly used to join strings (text)
first = "Hello"
second = "World"

result = first + " " + second
print(result)
Hello World

1. String (str)
A string is text (words, sentences)

Example:
name = "Ali"
msg = "Hello World"


2. Integer (int)
Whole numbers (no decimal)

Example:
age = 20
marks = 95


3. Float (float)
Numbers with decimal

Example:
price = 99.5
height = 5.8

4. Boolean (bool)
Only two values: True or False

Example:
is_passed = True

docs.python.org/3/library/functions.html
https://docs.python.org/3/library/stdtypes.html#text-sequence-type-str
https://docs.python.org/3/library/functions.html#print

print(*objects, sep=' ', end='\n', file=None, flush=False)¶
end='\n' it means new line.end=''it means no new line,,,,,,,,,, end="" e.g print("hello, ", end='')
you can use single or double qoute either.
* this means object can be from zero to unlimited objects, while separator is the space and end is showing that it has
completed and now new line will start. file and flush are keywords.
print is a function and everything within the parenthesis is argument or potential argument of the function.
These are also called the parameters of the function.
parameters and functions are same.

Positional Parameters:(print values to positional parameters)
First thing will print first and second thing will print second.
named parameters are end="" sep=' '
\ backslach is a escape character.

Format String:
for special string we use format string. or f string. 
to tell python to format stuff in string in special way
f is used in the beginning of the string
like   print(f"hello,{name}")
https://docs.python.org/3/library/stdtypes.html#string-methods

it is used to remove white spaces
name=name.strip() it is a method
name is a function . is a period strip is name of the function () open parenthesis and close parenthesis
name

it is used to capitalize the first letter
name = name.capitalize()
name= name.title()
to capitalize the first letter of every word.
if we want to remove space between two words then there are different function for this purpose.

integers:
These are just numbers and not decimals.like 1,2,-1,-2,+1,+2.

% it is called modulu operator
it takes reminder after dividing one number with the other number.

Interactive mode of the python
>>>


