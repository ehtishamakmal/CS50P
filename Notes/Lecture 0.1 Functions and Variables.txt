Integers:
✅ Insert new line below (without breaking line)
Windows / Linux: Ctrl + Enter
 puting one function into another function is called nest function.

complex code increases the probability of mistakes and technical mistakes and logical errors in your code.

Float:
Decimals in numbers are called floating numbers
https://docs.python.org/3/library/functions.html#round
round(number, ndigits=None)¶

f-string or formatting string:
An f-string (formatted string literal) lets you put variables or expressions directly inside a string using {}.
e.g print(f"My name is {name} and I am {age}")
x = 5
y = 3

print(f"Sum is {x + y}")

Output:
Sum is 8

When should you use f-strings?

Use them whenever you want to:

Combine variables into text
Print values nicely
Avoid messy + or .format()

def:
You can Use Your Own Functions or Defined Users Functions:
Indentation: means the spaces at the beginning of a line of code.
 In Python, it’s not just for style — it actually controls how the program works.
scope
return value








